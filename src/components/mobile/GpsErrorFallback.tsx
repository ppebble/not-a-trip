/**
 * GpsErrorFallback 컴포넌트
 * GPS 오류 시 폴백 UI
 * - 오류 유형별 메시지 표시
 * - 수동 지도 탐색 안내
 * - 설정 페이지 링크 (권한 거부 시)
 *
 * @requirements 1.5
 */

export default function GpsErrorFallback() {
  // TODO: Task 11.2에서 구현
  return null
}
