/**
 * BottomSheet 컴포넌트
 * 화면 하단에서 올라오는 UI 패널
 * - 드래그 핸들 UI
 * - 스냅 애니메이션 (collapsed/half/full)
 * - Safe Area 패딩 적용
 *
 * @requirements 1.2, 1.3, 4.4
 */

export default function BottomSheet() {
  // TODO: Task 9.4에서 구현
  return null
}
